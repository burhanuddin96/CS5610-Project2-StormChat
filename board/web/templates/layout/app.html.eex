<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Board</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>
  <body>
    <header class="header">
      <nav>
        <h1 class="branding"><a href="/">StormChat Alert Board</a></h1>
        <ul class="nav-links">
          <li><%= link("All topics", to: topic_path(@conn, :index)) %></li>
          <%= if @current_user do %>
            <li><%= link(@current_user.handle, to: profile_path(@conn, :show)) %></li>
            <li><%= link("Log out", to: session_path(@conn, :delete), method: :delete) %></li>
          <% else %>
            <li><%= link("Register", to: registration_path(@conn, :new)) %></li>
            <li><%= link("Log in", to: session_path(@conn, :new)) %></li>
          <% end %>
        </ul>
      </nav>
    </header>

    <!-- <aside class="notifications">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
    </aside> -->

    <main class="content">
      <%= render @view_module, @view_template, assigns %>
    </main>

    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
